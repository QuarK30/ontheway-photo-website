# ========== MongoDB ==========
# 本地开发（阶段二您使用的本地 MongoDB）：
# MONGODB_URI=mongodb://127.0.0.1:27017/ontheway
#
# 上线必须用云端数据库（后端在 Render 等平台，无法连您本机）：
# 从 MongoDB Atlas 或 Railway 获取连接串，将 <password> 换成实际密码（密码含特殊字符需 URL 编码）
# MONGODB_URI=mongodb+srv://用户名:密码@集群地址/数据库名?retryWrites=true&w=majority
MONGODB_URI=

# 服务端口（部署平台通常会注入 PORT，可不填）
PORT=4000

# JWT 密钥（登录签发 token 用，生产环境务必改为随机长字符串）
JWT_SECRET=your-secret-key-change-in-production

# 创建第一个管理员时使用（运行 npm run create-admin 前设置）
ADMIN_EMAIL=your@email.com
ADMIN_PASSWORD=your-password

# 可选：上线时填前端地址可限制 CORS，如 https://xxx.vercel.app（不设则允许所有来源）
# CORS_ORIGIN=
